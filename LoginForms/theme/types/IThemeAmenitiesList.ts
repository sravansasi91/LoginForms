export type ThemeAmenitiesList = {
  listBackground: string;
  openStatusColor: string;
  closedStatusColor: string;
  buttonColor: string;
  separatorColor: string;
  slotTypeColor: string;
  menuBackground:string;
  topListBackground : string;
  bottomListBackground : string;
  editButtonColor : string;
  editTextColor : string;
};
