export type ThemeDropdown = {
  popWindowBackgroundColor: string;
  backgroundColor: string;
  borderColor: string;
  iconColor: string;
  textColor: string;
  rowTextColor: string;
  rowBackgroundColor: string;
  rowDividerColor: string;
};
